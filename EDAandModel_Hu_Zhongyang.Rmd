---
title: "EDA_Hu_Zhongyang"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r }
final_fire=read.csv('final_wildfire.csv')
summary_nature=read.csv('summary_nature.csv')
summary_peoplecaused=read.csv('summary_peoplecaused.csv')
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
library(ggplot2)
hist(summary_nature$FIRE_SIZE)
hist(summary_peoplecaused$FIRE_SIZE)
table(final_fire$STAT_CAUSE_DESCR)

```


```{r}
boxplot(n~Year,summary_peoplecaused)
boxplot(n~month,summary_peoplecaused)

boxplot(n~Year,summary_nature)
boxplot(n~month,summary_nature)


```
```{r}
boxplot(FIRE_SIZE~Year,summary_peoplecaused)
boxplot(FIRE_SIZE~month,summary_peoplecaused)

boxplot(FIRE_SIZE~Year,summary_nature)
boxplot(FIRE_SIZE~month,summary_nature)

print('the mean of fire size of people caused is')
mean(summary_peoplecaused$FIRE_SIZE)
print('the mean of fire size of natural reasons is')
mean(summary_nature$FIRE_SIZE)


```
```{r}
boxplot(tair_day_livneh_vic~Year,summary_peoplecaused)
boxplot(tair_day_livneh_vic~month,summary_peoplecaused)

boxplot(soilmoist1_day_livneh_vic~Year,summary_peoplecaused)
boxplot(soilmoist1_day_livneh_vic~month,summary_peoplecaused)

boxplot(rainfall_day_livneh_vic~Year,summary_peoplecaused)
boxplot(rainfall_day_livneh_vic~month,summary_peoplecaused)
```
```{r}
library(stringr)
temp=str_remove(summary_nature$Budget,"[$]")
temp=str_remove_all(temp,"[,]")
summary_nature$Budget=as.numeric(temp)
temp=str_remove(summary_peoplecaused$Budget,"[$]")
temp=str_remove_all(temp,"[,]")
summary_peoplecaused$Budget=as.numeric(temp)
cor_nature=cor(summary_nature[c(4:9)])

library(corrplot)
corrplot(cor_nature,method='number')
cor_people=cor(summary_peoplecaused[c(4:9)])


corrplot(cor_people,method='number')

```


create model for nature reason

```{r}
model1=lm(n~tair_day_livneh_vic,data=summary_nature)
summary(model1)

model2=lm(n~tair_day_livneh_vic+soilmoist1_day_livneh_vic,data=summary_nature)

summary(model2)
model3=lm(n~tair_day_livneh_vic+soilmoist1_day_livneh_vic+rainfall_day_livneh_vic,data=summary_nature)
summary(model3)

library(car)
vif(model3)
vif(model2)
model4=lm(n~soilmoist1_day_livneh_vic,data=summary_nature)

summary(model4)

residualPlot(model2)

model5=lm(log(n)~tair_day_livneh_vic+soilmoist1_day_livneh_vic,data=summary_nature)
residualPlot(model5)
```