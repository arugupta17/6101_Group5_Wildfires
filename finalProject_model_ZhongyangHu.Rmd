---
title: "finalProject_Zhongyang_Hu_model"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
data1=read.csv('summary_nature.csv')
data1$totalarea=data1$n*data1$FIRE_SIZE
str(data1)
result=cor(drop_na(data1[c(4,5,6,7,8,10)]))
corrplot(result,method='number',type = 'lower', diag = TRUE)
hist(data1$totalarea)
summary(data1$totalarea)
model1=lm(log(totalarea)~tair_day_livneh_vic+soilmoist1_day_livneh_vic,data1)
summary(model1)
plot(model1)
model2=lm(log(FIRE_SIZE)~tair_day_livneh_vic+soilmoist1_day_livneh_vic,data1)
summary(model2)
plot(model2)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
